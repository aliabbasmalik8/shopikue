<p id="notice"><%= notice %></p>

<p>
  <strong>Image:</strong>
  <%= @image.image %>
</p>

<%= link_to 'Edit', edit_product_image_path(@imageable, @image) %> |
<%= link_to 'Back', product_images_path %>
